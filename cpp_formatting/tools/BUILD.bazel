# Test helpers for the integration tests.
load("@rules_cc//cc:cc_binary.bzl", "cc_binary")

package(default_visibility = ["//visibility:private"])

# A strict `git apply`: the tests must not depend on a system git.
cc_binary(
    name = "apply_patch",
    srcs = ["apply_patch.cpp"],
    visibility = ["//cpp_formatting:__pkg__"],
)
