# Vendored cpp_format integration kit.  Import it by URL (archive_override) or
# copy this directory into your repo, then
#   bazel run @cpp_formatting//bazel/integration:install
# drops cpp_format.sh into your workspace (default tools/cpp_format.sh); run that
# script: check | diff | fix | compile_commands | index | browse.
# See the README quick start.
load(":cpp_format.bzl", "cpp_format_install")

cpp_format_install(
    name = "install",
    visibility = ["//visibility:public"],
)

exports_files([
    "cpp_format.sh",
    "proto_index.bzl",
])
