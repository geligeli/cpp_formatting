name: Release

on:
  push:
    branches: [main]

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Cache Bazel repository cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/bazel
          key: bazel-${{ runner.os }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock') }}
          restore-keys: |
            bazel-${{ runner.os }}-

      - name: Build binary in devcontainer
        uses: devcontainers/ci@v0.3
        with:
          runCmd: |
            bazel build --config=minify //cpp_formatting:trailing_return_types
            cp -L bazel-bin/cpp_formatting/trailing_return_types trailing_return_types-linux-x86_64

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="$(date -u +%Y%m%d)-$(git rev-parse --short HEAD)"
          gh release create "$TAG" trailing_return_types-linux-x86_64 \
            --title "$TAG" \
            --notes "Built from commit $(git rev-parse HEAD)"
